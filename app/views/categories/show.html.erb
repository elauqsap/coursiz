<div class="container" id="mainNav">
  <div class="row-fluid">
    <div class="span2">
          <%= render 'layouts/sidenav' %>
        </div>
      <div class="span10">
        <div class="well">
          <table class="table table-condensed">
              <thead>
                <tr>
                  <th><%= sortable "quiz number" %></th>
                  <th><%= sortable "created_at", "created" %></th>
                </tr>
              </thead>
              <tbody>
              <% @cat.quizzes.each do |cat| %>
                <tr>
                  <td id="<%= cat.id %>"><a href="quizzes/<%= cat.id%>"><%= cat.id %></a></td>
                  <td><%= cat.created_at %></td>
                </tr>
              <% end %>
              </tbody>
            </table>
      </div>
      </div>
    </div>
  </div>
</div>