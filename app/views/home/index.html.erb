<div class="container" id="mainNav">
  <div class="row-fluid">
    <div class="span2">
          <%= render 'layouts/sidenav' %>
        </div>
      <div class="span10">
        <div class="hero-unit">
          <h2>Welcome <%= @users.name%>
          <hr>
          <% if !@enrolled_classes.nil? %>
            <h4>Your enrolled classes

          <% else %>
            <h4>Your are not enrolled in any classes

          <% end %>

      </div>
    </div>
  </div>
</div>